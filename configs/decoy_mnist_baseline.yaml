experiment:
  name: decoy_mnist
  seed: 0
dataset:
  name: decoy_mnist
  decoy: 4x4 swatch in random corner
model:
  name: conv_mnist
training:
  epochs: 10
  batch_size: 256
  learning_rate: 0.001
  weight_decay: 0.0001
rrr:
  enabled: false
  lambda: 0.0
